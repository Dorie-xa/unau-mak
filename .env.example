# PostgreSQL connection string (Vercel Postgres / Neon / Supabase all work)
DATABASE_URL="postgresql://user:password@host:5432/unau_mak?sslmode=require"

# Long random string used to sign admin session cookies
SESSION_SECRET="replace-with-a-long-random-string"

# Created automatically when you add Vercel Blob storage to your project
BLOB_READ_WRITE_TOKEN=""

# Used to seed the two admin accounts (prisma/seed.ts) — change these
ADMIN1_EMAIL="patricia@unaumak.org"
ADMIN1_PASSWORD="ChangeMe123!"
ADMIN1_NAME="Patricia N."
ADMIN2_EMAIL="david@unaumak.org"
ADMIN2_PASSWORD="ChangeMe123!"
ADMIN2_NAME="David K."

# Optional: confirmation emails for signup + project registration.
# Without these set, the app still works — it just skips sending email
# and logs a note to the server console instead.
RESEND_API_KEY=""
MAIL_FROM="UNAU Mak Chapter <onboarding@resend.dev>"